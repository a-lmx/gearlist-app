<% content_for :sidebar do %>
  <%= render partial: 'shared/user_lists' %>
<% end %>

<%= render partial: 'shared/list', locals: { list: @list } %>

<% if authorized? %>
  <%= link_to 'Add Items', new_list_item_path(params[:id]), class: 'btn btn-default' %>
<% end %>
